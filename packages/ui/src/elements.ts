/**
 * MeowTileKit UI 的「元素」子入口：Web Components。
 *
 * 引入即自动注册 `<mkt-map>` / `<mkt-search>` / `<mkt-coord-panel>` /
 * `<mkt-legend>` / `<mkt-settings>` / `<mkt-scale>` / `<mkt-output>` / `<mkt-info>`。
 */

export * from './index.js';
export * from './elements/index.js';
