/**
 * MeowTileKit（伞包）根入口 = core + ui。
 *
 * 大多数人装 `meow-tile-kit` 即可；只要数据用 `meow-tile-kit-core`，
 * 只要界面用 `meow-tile-kit-ui`，自定义元素见 `meow-tile-kit/elements`。
 */

export * from 'meow-tile-kit-core';
export * from 'meow-tile-kit-ui';
